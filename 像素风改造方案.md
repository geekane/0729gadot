# 🦇 哥谭大冒险 — 像素风改造完整方案

> 基于当前纯 GDScript 矢量代码的完整像素化改造分析、方案设计、代码评估与迭代建议。

---

## 目录

1. [代码现状全景分析](#1-代码现状全景分析)
2. [像素风改造核心策略](#2-像素风改造核心策略)
3. [分层改造方案详解](#3-分层改造方案详解)
4. [纯代码像素图方案](#4-纯代码像素图方案)
5. [内部评估与迭代分析](#5-内部评估与迭代分析)
6. [推荐执行路径](#6-推荐执行路径)
7. [附录：文件变更清单](#7-附录文件变更清单)

---

## 1. 代码现状全景分析

### 1.1 项目文件总览

| 文件 | 总行数 | `_draw()` 绘制 | 视觉节点创建 | 碰撞体 | 改造影响 |
|------|--------|---------------|------------|--------|---------|
| `Player.gd` | 324 | **119行** (47-165) | 0 | 24×36 Rect | 🔴 核心角色 |
| `Enemy.gd` | 152 | **28行** (35-62) | 0 | 28×24 Rect | 🔴 |
| `FlyEnemy.gd` | 117 | **27行** (56-82) | 0 | r=16 Circle | 🔴 |
| `Coin.gd` | 108 | **30行** (54-84) | 0 | r=16 Circle | 🟡 |
| `Hazard.gd` | 56 | **24行** (27-50) | 0 | 40×16 Rect | 🟢 简单 |
| `MovingPlatform.gd` | 47 | 0 | ColorRect×2 | 120×16 Rect | 🟢 简单 |
| `Game.gd` | 917 | 0 | ColorRect×N | 无碰撞 | 🟡 背景多 |
| `TestRunner.gd` | 524 | 0 | 0 | 无 | ✅ 不改 |
| **总计** | **~2245** | **~228行绘制** | | | |

### 1.2 当前角色绘制方式

| 角色 | 绘制层数 | 绘制方法 | 动画驱动 | 刷新控制 |
|------|---------|---------|---------|---------|
| 🦇 蝙蝠侠 | **9层** | draw_rect/circle/polygon/line | sin(anim_time)×振幅 相位 | 30FPS隔帧 |
| 🍄 蘑菇怪 | **6层** | draw_rect/circle/polygon/line | direction→flip 切换 | 转向时触发 |
| 🚁 无人机 | **5层** | draw_circle/line/polygon | sin(anim_time) 波浪+旋转 | 每帧重绘 |
| 🪙 金币 | **5层** | draw_circle/polygon 椭圆 | sin(anim_time) 浮动+旋转 | 每帧重绘 |
| ⚔️ 地刺 | **1层** | draw_polygon 三角形×3 | 静态 | 一次性 |

### 1.3 场景背景元素（Game.gd 世界构建）

| 元素 | 实现方式 | 颜色/尺寸 | 数量 |
|------|---------|-----------|------|
| 天空底板 | ColorRect | `0.12,0.16,0.28` | 1 |
| 明月 | ColorRect | 65×65 暖色 | 1 |
| 柔和浮云 | ColorRect | 半透明 | 5~10 |
| 摩天大楼 | ColorRect | 140×220 深色 | 依关卡宽度 |
| 大楼窗户 | ColorRect | 10×14 暖色半透明 | 每栋1个 |
| 蝙蝠探照灯 | Polygon2D | 锥形半透明 | 1 |
| 草地 | ColorRect | 全宽×6px 绿色 | 1 |
| 地面 | StaticBody2D 子 ColorRect | 棕色 | 1 |

### 1.4 碰撞体与视觉错位风险

碰撞体是命脉，改造中**必须保持不变**：

| 节点 | 碰撞体尺寸 | 偏移基准 | 像素图建议尺寸 |
|------|-----------|---------|--------------|
| Player | 24×36 | 中心 | 24×36 (或 48×72 后 2x缩放) |
| Enemy | 28×24 | 中心 | 28×24 |
| FlyEnemy | r=16 | 中心 | 32×32 |
| Coin | r=16 | 中心 | 32×32 |
| Hazard | 40×16 | 中心偏上 | 40×16 |

---

## 2. 像素风改造核心策略

### 2.1 架构原则：渲染层剥离

当前架构是**逻辑+绘制紧耦合**，改造核心是拆成两层：

```
改造前:
  Enemy.gd
    ├── _physics_process(delta)     ← 逻辑
    └── _draw()                     ← 绘制（同文件）

改造后:
  Enemy.gd
    ├── _physics_process(delta)     ← 逻辑（完全不变）
    └── 子节点: AnimatedSprite2D    ← 绘制（独立节点）
```

**不变的部分**（不改一行）：
- 物理引擎 `move_and_slide()` / `velocity`
- 碰撞体 `CollisionShape2D`
- 状态机（GameState）
- 关卡数据（LEVEL_CONFIGS）
- 信号连接（body_entered）
- 得分/生命/暂停系统

**要改的部分**：
- `_draw()` 全部删除
- `queue_redraw()` 全部删除
- 新增 `AnimatedSprite2D` 或 `Sprite2D` 子节点
- 方向控制由 `flip` → `flip_h`

### 2.2 像素渲染设置

```gdscript
# PixelConfig.gd
static func apply(viewport: Viewport):
    # 最近邻滤波——核心！否则像素图边缘模糊
    viewport.canvas_item_default_texture_filter = Viewport.DEFAULT_TEXTURE_FILTER_NEAREST
    
    # 整数缩放避免亚像素偏移
    viewport.scaling_3d_scale = 1.0
    
    # 可选：固定分辨率
    # viewport.size = Vector2i(1152, 648)
```

### 2.3 三种纯代码实现方案

| 方案 | 原理 | 效果 | 代码量 | 推荐度 |
|------|------|------|--------|--------|
| **A: 降采样法** | 现有_draw()渲染到小Viewport→NEAREST放大 | 矢量变马赛克，细节糊 | ~50行 | ❌ |
| **B: Image逐像素** | 用Image.set_pixel()逐像素构建纹理 | **真像素风** | ~2500行 | ⭐ 最终目标 |
| **C: 混合策略** | 背景/UI像素化 + 角色保留矢量 | 部分像素感 | ~300行 | ⭐⭐ 起步推荐 |

---

## 3. 分层改造方案详解

### 3.1 基础设施层（Phase 0）

**新增 `res://PixelConfig.gd`** — 全局像素设置

```gdscript
# 像素风全局渲染配置
static func apply(viewport: Viewport):
    viewport.canvas_item_default_texture_filter = Viewport.DEFAULT_TEXTURE_FILTER_NEAREST
```

**新增 `res://PixelSprite.gd`** — 像素精灵工具工厂

```gdscript
# 职责：统一创建像素精灵，保证 NEAREST 滤波
static func from_texture(path: String) -> Sprite2D:
    var tex = load(path)
    tex.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST
    var sprite = Sprite2D.new()
    sprite.texture = tex
    return sprite

static func animated_from_frames(path: String) -> AnimatedSprite2D:
    var res = load(path)
    var anim = AnimatedSprite2D.new()
    anim.sprite_frames = res
    anim.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST
    return anim
```

### 3.2 背景改造（Phase 1 — 效果最明显）

#### 3.2.1 程序化像素大楼

```gdscript
func _generate_pixel_buildings(w: int, h: int, seed_val: int) -> Texture2D:
    var img = Image.create(w, h, false, Image.FORMAT_RGBA8)
    img.fill(Color(0.16, 0.2, 0.32))  # 墙体底色
    
    # 像素窗户（6×5 格子，间隔 4px）
    for bx in range(4, w - 4, 10):
        for by in range(6, h - 6, 9):
            var lit = (bx * 7 + by * 13 + seed_val) % 5 != 0  # 伪随机亮灯
            var color = Color(1.0, 0.88, 0.35, 0.7) if lit else Color(0.1, 0.12, 0.2, 0.5)
            for px in range(bx, min(bx + 4, w)):
                for py in range(by, min(by + 3, h)):
                    img.set_pixel(px, py, color)
    return ImageTexture.create_from_image(img)
```

#### 3.2.2 程序化像素月亮

```gdscript
func _generate_pixel_moon(size: int) -> Texture2D:
    var img = Image.create(size, size, false, Image.FORMAT_RGBA8)
    img.fill(Color.TRANSPARENT)
    var cx = size / 2.0
    var cy = size / 2.0
    var r = size / 2.0 - 1
    for x in range(size):
        for y in range(size):
            var dx = x - cx
            var dy = y - cy
            if dx*dx + dy*dy <= r*r:
                img.set_pixel(x, y, Color(1.0, 0.95, 0.7, 0.9))
    return ImageTexture.create_from_image(img)
```

### 3.3 玩家改造（Phase 2 — 最复杂）

#### 3.3.1 像素图数据数组定义

```gdscript
# res://player_pixel_data.gd
# 蝙蝠侠 12×18 像素图（碰撞体 24×36，每像素=2px，2x NEAREST 放大）

static var IDLE_FRAMES = [
    # 帧1 — 待机
    [
        "....DDDD....",   # 头盔顶
        "...DDBBD...",    # 白眼
        "...DDDDDD...",
        "....QTTQ....",   # Q=头盔边缘, T=肤色下巴
        "....YLLY....",   # Y=黄色胸章, L=浅色
        "...YYYYYY...",
        "...PPPPPPPP...", # P=手臂
        "....SSSS....",   # S=腰带
        "....SSSS....",
        "...K..K...",    # K=深色腿
        "...K..K...",
        "...B..B...",    # B=靴子
    ],
    # 更多帧...
]
```

#### 3.3.2 从数据数组生成纹理

```gdscript
# 像素图工具：字符串数组 → Texture2D
func array_to_texture(data: Array[String], palette: Dictionary) -> Texture2D:
    var h = data.size()
    var w = data[0].length()
    var img = Image.create(w, h, false, Image.FORMAT_RGBA8)
    for y in range(h):
        var row = data[y]
        for x in range(min(row.length(), w)):
            var char = row[x]
            var color = palette.get(char, Color(0, 0, 0, 0))
            img.set_pixel(x, y, color)
    var tex = ImageTexture.create_from_image(img)
    tex.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST
    return tex
```

#### 3.3.3 动画帧估算

| 动画 | 帧数 | 每帧像素尺寸 | 总像素数据行数 |
|------|------|-------------|--------------|
| idle | 4 | 12×18 | 72行 |
| run | 6 | 12×18 | 108行 |
| jump_up | 3 | 12×18 | 54行 |
| jump_fall | 2 | 12×18 | 36行 |
| hurt | 2 | 12×18 | 36行 |
| **小计** | **17帧** | | **~306行** |

### 3.4 敌人改造（Phase 3）

#### 3.4.1 蘑菇怪像素图（14×12）

| 行列 | 像素排列 | 说明 |
|------|---------|------|
| 0 | `"....DDDD...."` | 头顶暗色 |
| 1 | `"...WWDWW..."` | 白眼 |
| 2 | `"...wBDw..."` | 瞳孔随方向 |
| 3 | `"..RRRRRR.."` | 红菇头 |
| 4 | `"..RRRRRR.."` | 红菇身体 |
| 5 | `".LL..LL."` | 腿 |

#### 3.4.2 无人机像素图（16×16）

| 动画 | 帧数 | 关键变化 |
|------|------|---------|
| fly | 4 | 螺旋桨叶片旋转 |
| explode | 4 | 爆炸扩散 |

### 3.5 道具改造（Phase 4）

#### 3.5.1 金币旋转

8×8 像素，8帧旋转动画，模拟椭圆旋转时的宽度变化：

```
帧0: "..RR.."  帧1: "..RR.."  帧2: ".RRR."  帧3: "RRRR"
帧4: ".RRR."  帧5: "..RR.."  帧6: "..RR.."  帧7: "..RR.."
```

#### 3.5.2 地刺（静态）

20×10 像素，单帧，纯三角形锯齿：

```
"....RR........RR..."
"...RRRR......RRRR.."
"..RRRRRR....RRRRRR."
".RRRRRRRR..RRRRRRRR"
"RRRRRRRRRRRRRRRRRRRR"
```

### 3.6 UI 改造（Phase 5）

```
当前:  "💰 " + str(score)     ← Emoji 渲染，不同系统不一致
改造:  像素图标 Sprite + BitmapFont 数字

当前:  "❤️ ❤️ ❤️"           ← Emoji
改造:  HeartIcon._draw() 或像素心形 Sprite×3
```

**HeartIcon.gd**（纯代码像素心形，替代 Emoji）：

```gdscript
extends Sprite2D

func _ready():
    var img = Image.create(8, 8, false, Image.FORMAT_RGBA8)
    # 像素心形图案
    var hearts = [
        ".RR..RR.",
        "RRRRRRRR",
        "RRRRRRRR",
        ".RRRRRR.",
        "..RRRR..",
        "...RR...",
        "....R....",
    ]
    for y in range(hearts.size()):
        for x in range(hearts[y].length()):
            if hearts[y][x] == 'R':
                img.set_pixel(x + 1, y + 1, Color(1.0, 0.2, 0.2))
    texture = ImageTexture.create_from_image(img)
    texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST
```

---

## 4. 纯代码像素图方案

### 4.1 三种方案对比

| | A: 降采样法 | B: Image逐像素 | C: 混合策略 |
|---|---|---|---|
| **原理** | 现有_draw()下采样+Sacle | Image.set_pixel()造纹理 | 背景像素化+角色保留矢量 |
| **像素感** | ❌ 模糊马赛克 | ✅ 真像素风 | ⚠️ 半像素半矢量 |
| **代码量** | ~50行 | **~2500行** | ~300行 |
| **角色动效** | 保留 | 离散帧 | 保留流畅 |
| **执行难度** | ⭐ 简单 | ⭐⭐⭐ 体力活 | ⭐ 简单 |
| **维护性** | 好 | 像素图改起来麻烦 | 好 |
| **推荐** | ❌ 不推荐 | ⭐ 终极目标 | ⭐⭐ 起步推荐 |

### 4.2 推荐执行路径：C → B 渐进式

```
第1步 ─── 场景背景纯代码像素化
         大楼/云/月亮/地面 → Image生成像素图
         ~300行代码，效果最明显

第2步 ─── UI 替换为像素图标
         HeartIcon / CoinIcon / CrownIcon
         ~100行代码，彻底消除 Emoji 乱码

第3步 ─── 金币/地刺改像素图
         简单形状，Image 画
         ~150行代码

第4步 ─── 敌人改像素图
         数据数组定义蘑菇怪/无人机
         ~400行代码

第5步 ─── 玩家改像素图
         17帧 12×18 数据数组
         ~1000行代码，最后做最复杂的
```

每个步骤**独立可测试**，随时可以停下来验收效果。

### 4.3 程序化像素图工具库（PixelLib.gd）

```gdscript
# res://pixel_lib.gd — 纯代码像素图生成工具
static func create_texture(width: int, height: int, pixels: Array[String],
                           palette: Dictionary) -> Texture2D:
    var img = Image.create(width, height, false, Image.FORMAT_RGBA8)
    assert(pixels.size() == height, "像素行数必须等于高度")
    for y in range(height):
        var row = pixels[y]
        for x in range(min(row.length(), width)):
            var char = row.substr(x, 1)
            if palette.has(char):
                img.set_pixel(x, y, palette[char])
    var tex = ImageTexture.create_from_image(img)
    tex.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST
    return tex

static func create_anim_textures(frames_data: Array[Array], palette: Dictionary,
                                 frame_w: int, frame_h: int) -> Array[Texture2D]:
    var textures: Array[Texture2D] = []
    for data in frames_data:
        textures.append(create_texture(frame_w, frame_h, data, palette))
    return textures
```

使用示例：

```gdscript
const PALETTE = {
    "D": Color(0.1, 0.1, 0.14),  # 深色
    "B": Color(1.0, 1.0, 0.95),  # 白色
    "T": Color(0.95, 0.78, 0.65), # 肤色
    "Y": Color(1.0, 0.85, 0.1),  # 金黄色
    "S": Color(1.0, 0.8, 0.1),   # 腰带宽
}

var tex = PixelLib.create_texture(12, 18, IDLE_FRAME_1, PALETTE)
```

---

## 5. 内部评估与迭代分析

### 5.1 评估矩阵

| 维度 | 评分 (1-5) | 说明 |
|------|-----------|------|
| **技术可行性** | 4.5 | Godot 对像素风原生支持好，代码分层清晰 |
| **改造量** | 2.5 | ~228行 `_draw()` 替换为 ~2500行像素数据，纯体力 |
| **风险** | 3.0 | 主要风险来自像素图质量，不是代码 |
| **碰撞体安全** | 4.5 | 碰撞体不动，只换视觉层，物理完全不变 |
| **性能收益** | 4.0 | Sprite 渲染比 draw_polygon 更高效 |
| **维护成本** | 3.5 | 像素数据内联在代码中，不如外部图片好替换 |

### 5.2 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 像素图"像素感"不足 | 中 | 中 | 统一用 12×18 小尺寸+NEAREST 2x~4x放大 |
| 碰撞体与像素图错位 | 低 | 高 | 像素图尺寸必须与碰撞体一致或整数倍 |
| 动画过渡不如矢量流畅 | 中 | 中 | 关键帧数要足够（idle≥4帧, run≥6帧） |
| 背景改造工作量大 | 中 | 中 | 5层背景可分阶段渐进，不要求一次到位 |
| TestRunner 像素采样误报 | 低 | 低 | 调整 WHITE_THRESHOLD 参数即可 |
| 配色不协调 | 中 | 低 | 先定义统一色板 palette，保持一致 |

### 5.3 工作量估算

| 阶段 | 内容 | 代码量 | 人天 |
|------|------|--------|------|
| Phase 0 | 基础设施 PixelConfig + PixelLib | ~80行 | 0.5天 |
| Phase 1 | 场景背景像素化 | ~300行 | 1天 |
| Phase 2 | 玩家改造（17帧数据） | ~1000行 | 2天 |
| Phase 3 | 敌人改造 | ~400行 | 1天 |
| Phase 4 | 金币+地刺改造 | ~150行 | 0.5天 |
| Phase 5 | UI 像素化（HeartIcon/BitmapFont） | ~200行 | 0.5天 |
| Phase 6 | 集成测试+调优 | ~100行 | 1天 |
| **总计** | | **~2230行** | **~6.5天** |

### 5.4 迭代改进点

1. **不要一次改完** → 分 6 个独立 Phase，每个可单独测试
2. **用 Image.create() 做占位图** → 没有真实像素图时也能验证代码逻辑是否正确
3. **碰撞体复用策略** → 像素图尺寸必须与碰撞体一致或整数倍，否则连锁修改物理
4. **先背景后角色** → 背景改动代码少、效果好、建立信心后再改复杂的角色
5. **统一色板先行** → 先定义全局调色板，确保所有角色场景色彩协调

---

## 6. 推荐执行路径

```
Phase 0 ─── 基础设施（立即见效）
  ├── 创建 res://pixel_lib.gd     ← 像素图工具库
  ├── 创建 res://pixel_config.gd   ← 渲染设置
  └── Game.gd._ready() 调用 PixelConfig.apply()

Phase 1 ─── 场景背景像素化（效果最明显）
  ├── 大楼 → _generate_pixel_buildings()
  ├── 月亮 → _generate_pixel_moon()
  ├── 浮云 → _generate_pixel_cloud()
  └── 测试：肉眼可见背景像素化

Phase 2 ─── 道具+UI
  ├── 金币 8×8 像素旋转动画
  ├── 地刺 20×10 静态像素图
  ├── HeartIcon 像素心形
  └── 测试：采集/受伤显示正常

Phase 3 ─── 敌人
  ├── 蘑菇怪 14×12 walk/stomp 数据定义
  ├── 无人机 16×16 fly/explode 数据定义
  └── 测试：踩头判定不变

Phase 4 ─── 玩家（最复杂最后做）
  ├── 蝙蝠侠 12×18 四动画数据定义
  ├── _draw() 删除，AnimatedSprite2D 接入
  └── 测试：全流程通关

Phase 5 ─── 收尾
  ├── TestRunner 跑一轮
  ├── 调整像素缩放比例
  └── 打包验证
```

---

## 7. 附录：文件变更清单

### 新增文件（3个）

| 文件 | 行数 | 职责 |
|------|------|------|
| `res://pixel_lib.gd` | ~100 | 像素图生成工具库 |
| `res://pixel_config.gd` | ~20 | 全局像素渲染设置 |
| `res://pixel_palette.gd` | ~30 | 统一色板定义 |

### 修改文件（6个）

| 文件 | 改动量 | 改动内容 |
|------|--------|---------|
| `Game.gd` | ~300行 | 背景生成替换为像素图 + 引用 PixelConfig |
| `Player.gd` | ~1000行 | 删除 _draw()，新增像素数据+AnimatedSprite2D |
| `Enemy.gd` | ~200行 | 删除 _draw()，新增像素数据 |
| `FlyEnemy.gd` | ~150行 | 删除 _draw()，新增像素数据 |
| `Coin.gd` | ~100行 | 删除 _draw()，新增像素旋转数据 |
| `Hazard.gd` | ~60行 | 删除 _draw()，新增静态像素图 |

### 无变更文件（2个）

| 文件 | 原因 |
|------|------|
| `TestRunner.gd` | 纯测试框架，无视觉代码 |
| `MovingPlatform.gd` | ColorRect 视觉本身接近像素风，可不改 |

---

> **结论**：纯代码像素化技术上完全可行，核心工作量在像素数据数组定义（~2500行体力活），推荐混合策略（C→B 渐进式）先从背景开始做出效果，最后再改造玩家角色。
